services:
  api:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    command: pnpm run start:dev
    environment:
      - CHOKIDAR_USEPOLLING=true